Your job is to extract information from resume. Please try to keep bullet points the same, and don't split one bullet point into multiple or merge them. make sure you don't miss any bullet points. There may be typos in the resume, please fix those without changing the surrounding text. Do not use Markdown. Use only ASCII characters. If date is a range, ensure that there is a "-" in the middle (for example, don't say Jan 2021 Aug 2024 but rather Jan 2021 - Aug 2024). It is okay for certain values to not exist, for those, simply don't include those attributes. Respond only in JSON. Do not include any explanations, text, or formatting outside of the JSON object. Output json content in the following format: